nginx:
    build: ./nginx
    ports:
      - "8080:80"
    links:
      - "php"
    volumes:
      - ~/labs/docker-php/opt:/opt
      - ~/labs/docker-php/opt/nginx/nginx.conf:/etc/nginx/nginx.conf
      - ~/labs/docker-php/opt/nginx/vhost:/etc/nginx/conf.d

php:
    build: ./php
    ports:
      - "9000:9000"
    links:
      - "mysql"
    volumes:
      - ~/labs/docker-php/opt:/opt

mysql:
    image: mysql:5.7
    ports:
      - "3308:3306"
    volumes:
      - mysql-data:/var/lib/mysql
    environment:
      MYSQL_ROOT_PASSWORD: 123456